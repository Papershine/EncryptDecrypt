import SwiftUI

@main
struct MyApp: App {
    var body: some Scene {
        WindowGroup {
            // Start the navigation control
            NavigationView().preferredColorScheme(.light)
        }
    }
}
